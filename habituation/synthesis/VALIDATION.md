# 语言修订版验证记录

## 产物

- `habituation/report.pdf`：22页，374102字节。
- SHA-256：`4551a9411e78be1b117b53a29e6c5362e5e74cdbae87cddb5b38eb325dc4e077`。
- 5幅矢量图、17张表、10条参考文献、39个书签、23项嵌入字体。
- 上一统一稿为提交 `50b2f15`。本次重写摘要、正文、图注、讨论和网页摘要，删除三张重复的概述表；实验数据表和曲线数据保留。

## 数据与科学内容

1. 与修订开始时的快照比较，33个图表数据、数据清单、汇总、生成脚本及文献文件逐字节相同。
2. 原数据准备程序重新运行，29个来源文件及冻结预测/模型的哈希检查通过。窗口构成仍为1006，其中6个空白检查窗；1212为此前审计的事件文件数，11为种子标识数。
3. `preserved_sources.json`中的167个历史文件均未改变，包含三个分阶段PDF和首轮源码ZIP。
4. PDF文本核对保留模型规模、首轮D、初次下降、状态干预响应、代理误差、H3归一化差别、H4(b)原比较条件不足及结果驱动补测、H5效应量阈值、H6相同计数、H7无法判断与H8未见增益的区别。
5. 没有新增仿真、拟合、参数选择或统计检验，也未重新回读全部原始事件。正文所述32项实验单元测试为实验材料首次发布时的记录。

## 文本与构建

- 目录改为常规章节标题，删去Q1–Q5、章节口号及重复的结尾总结。移除“本节推进的认识”“解释关”“证据链”“冒充”等表述，逐段核读实验描述与结论。
- `validate.py`的文字存在检查改为修订稿中的对应表述；数值、来源哈希、历史文件、字体、书签和版面检查保留。
- 最终Tectonic构建无Overfull、缺字、未解析引用或交叉引用。系统字体绝对路径的可移植性提示仍存在，因此不承诺跨字体环境的PDF字节一致。
- qpdf结构检查通过；字体全部嵌入；文本无替换字符，逐词边界检查无页外溢出。
- 最终22页均已渲染并查看总览，另外检查目录、正文和图表页面。目录单页完整，附录判据标题与表格同页；参考文献条目不在页间拆开。
- Markdown导航链接、Python语法、PDF主要数值及本地路径泄露检查通过；`git diff --check`通过。

自动检查见 `validation.json`，构建来源见 `build_manifest.json`。上述检查针对报告修订，不构成新的生物学验证。重新编译可能改变PDF时间元数据和哈希，届时需更新此记录。
