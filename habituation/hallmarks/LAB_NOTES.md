# 补测执行与偏离记录

## 启动前

读取工作区指南、原始实验入口及冻结协议，确认上游和旧结果不变。保存本轮PROTOCOL、JSON及SHA-256后，以单进程先运行第二刺激部分，再运行主实验。没有使用子agent或并发全脑任务。

## 第二刺激实测

`results/novel_v1`完成，181.07秒；72个读出窗口、135个完整事件文件（包括静默间隔）。两种B（JO-F、上游neu_JON_D_m）在三个实现中都不能直接驱动aBN1，却产生165–292个非输入神经元脉冲。插入B后的A读出与等时休息对照均相同，B→空白窗口aBN1均为0。

独立回读通过，所有实际感觉事件符合冻结输入接口。H7缺少可比较的B基线，不能判为通过或阴性；H8满足刺激有效性/空白门槛，但未出现解除增益。H9没有达到触发条件，不重复无效解除来伪装检验。

首次分析在形成结果JSON前因漏导入 `datetime.timezone` 抛出NameError。修复分析器导入后重读同一数据，未重跑神经实验，也没有改变任何神经结果或阈值。保留修复事实；这不是Brian2模型崩溃。早期第二刺激分析入口和指标源码另存到 `analysis/novel/*_snapshot.py`。

## 主实验前的恢复指标补充

在主实验目录出现前保存 `RECOVERY_INTERPRETATION.md` 及时间/哈希证据。保留原先F50筛查，同时报告恢复到首次80%的采样区间。F分母不同导致的顺序变化不能被包装成H4(b)的完整复现。

## 文献驱动的H3限定扩展

复核Smart et al. 2024 §1.4.1时确认：其H3数学表述允许部分恢复，残余状态造成的后续响应降低也在其范围内。原主协议的10秒＋80%恢复门槛更严格，因此不能凭该协议阴性否定所有H3。

在尚未读取主实验的多轮和恢复输出时，保存2秒休息补充方案和JSON；当时已看到第二刺激实验中的单轮A基线。只增加这一个固定条件，不扫描休息时长。分别报告绝对响应/共同基准的下降与相对每轮自身初始响应的下降，不将残余状态等同于长期学习或元可塑性。原10秒协议与判据不变。

## 主实验与部分恢复补充完成

主实验2225.82秒，261个响应窗口、513个完整事件文件；2秒H3补充430.17秒，108个响应窗口、213个完整事件文件。10秒原筛查0/3，2秒补充3/3；H5方向3/3一致但仅2/3达到≥0.10的D差；H6三例的五点恢复计数差全部为0。独立回读保存于 `analysis/full/`，此份分析和入口/指标快照均保留。

所有感觉输入逐事件一致；主程序、B接口程序、部分恢复程序的第一轮完整训练事件哈希相同。连续H3第二轮的第一个刺激，与同种子主实验独立2秒恢复分支的全脑事件相同，进一步检查恢复分叉没有改变物理结果。

## H4(b)资格补救与停止

原12次比较只有3702合格；3701慢组平台差0.667>0.600，3703慢组D=29.4%<30%。没有改阈值。另冻结 `FREQUENCY_SUPPLEMENT.md`，只增加T=0.75秒、24次，与原T=0.5秒、24次匹配。该设计是在看到原资格结果之后作出的，明确不伪装成最初主方案。

新增557.79秒、84个响应窗口、165个事件文件，三例都达到原平台/递减门槛。F50采样区间均未分开；首次80%只在3702高频更早（较低频组5秒探测为7，未达到首次9的80%）。不能将单个实现的结果宣称为稳健建立H4(b)。完成这一限定补测后停止扩大，不继续搜索条件。

最终审计另存 `analysis/final/`，全部1026个事件文件、525个观测窗口、519个感觉事件序列检查通过；其中6个观测窗为空白检查。501个静默区间未出现脉冲，但这不等于重置状态。成功批次累计3394.84秒，即56.58分钟，在90分钟预算内。所有全脑进程均已结束；无子agent参与、无提交或推送。

## 报告制作

数据驱动生成文字表格和6页LaTeX PDF。首次排版有一处恢复图宽度溢出4.74pt，缩小图幅后消除；没有改变数据或指标。旧Fontconfig配置兼容提示仍存在，但最终无缺字、未解析引用、Overfull或annotation错误。原两份PDF及首轮源码包不覆盖。
