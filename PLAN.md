# 数字果蝇：复现与连接组约束训练计划

记录日期：2026-09-16。

## 目标与授权

用户希望先理解并运行数字果蝇大脑，再探索它与 LLM/LMM、普通神经网络的区别，以及参数训练框架。此前仅做资料调研；本次已明确授权：记录计划，并开始第一条「全脑复现线」的本地实现和运行。

本阶段不接入虚拟身体、不开展全脑权重训练、不宣称实现完整数字孪生或意识上传。先建立可重复、可审计的神经动力学基线。

## 两条路线

### 路线 A：Shiu/Brian2 全脑复现（当前执行）

基于 Shiu 等《A Drosophila computational brain model reveals sensorimotor processing》（Nature, 2024）的官方实现。

1. 检查主机资源，建立项目独立环境，不改系统 Python。
2. 固定上游提交和 FlyWire v630 数据；保留上游源码不变。
3. 记录依赖版本、数据校验和、随机种子、仿真时间步和硬件。
4. 首先运行低并发短时间 smoke test。
5. 运行全脑无输入对照、糖感受神经元刺激、糖＋苦味刺激；读取论文所用 MN9 输出。
6. 保存脉冲数据、逐试验统计、日志及结果摘要；区分“运行成功”“定性复现”和“完整论文复现”。
7. 在资源允许时增加随机重复，验证糖刺激引起进食相关输出、苦味共刺激抑制输出。
8. 基线确认后，再研究少量参数的敏感性和拟合。该训练工作不属于此次首轮复现的默认范围。

首轮验收：原始全脑模型实际运行，数据/细胞 ID 对齐，输出可审计；关键响应方向与论文一致。默认 30 次重复、164 项预测及论文全部图表不视为已经复现。

### 路线 B：Flyvis 参数训练方法（后续）

参考 Lappalainen 等的连接组约束视觉模型及其 PyTorch 实现：固定连接结构，按细胞类型与连接类型共享参数，通过视觉运动任务训练，并用独立生理实验验证。

Flyvis 是视觉子系统、非脉冲动力学模型，不等同于原始全脑 LIF 模型。论文的 734 个神经模型自由参数不包含外部解码器的全部参数。

## 全脑模型的参数与状态

连接权重：W_ij = 突触数量 N_ij × 兴奋/抑制符号 s_i × 全局强度 w_syn。

| 参数 | 原始默认值 | 意义 |
|---|---:|---|
| v_0 | -52 mV | 静息电位 |
| v_rst | -52 mV | 重置电位 |
| v_th | -45 mV | 发放阈值 |
| t_mbr | 20 ms | 膜时间常数 |
| tau | 5 ms | 突触驱动衰减时间 |
| t_rfc | 2.2 ms | 不应期 |
| t_dly | 1.8 ms | 传递延迟 |
| w_syn | 0.275 mV | 全局突触强度 |

原论文仅校准 w_syn，其余参数主要来自生理或建模先验；不代表其他参数不能优化。

刺激细胞、刺激频率、持续时间是实验条件；积分步长、重复次数、并行数是仿真配置；膜电位、突触驱动是动态状态。状态变化不等于学习。原模型没有长期可塑性机制，基础放电假定为零。

## 与 LLM/LMM 的比较视角

本模型本身也是神经网络：真实连接组约束的稀疏循环脉冲网络。与典型 Transformer 相比，结构来源、显式生物时间、细胞可解释性和研究目标不同。它不是天然的语言模型；结构测量不能直接视为生前记忆或语言预训练权重。

比较实验要控制参数量、运行成本、输入/输出接口及训练数据，不以游戏演示或外部控制器表现替代神经科学验证。

## 后续训练框架设想

按逐级开放原则：

0. 脑冻结，只训练读出，用于判断已有活动的信息量。
1. 优化全局突触增益、兴奋/抑制比例等少量参数。
2. 按细胞类型共享阈值差、时间常数等参数。
3. 按连接类型训练正值增益，保留连接掩码及符号约定。
4. 在选定子回路开放逐边权重或可塑性规则。
5. 最后才考虑全脑广泛参数优化。

方法：低维无梯度优化；可微模型/BPTT；脉冲替代梯度；在线资格迹/奖励调制学习。GPU 仿真加速不等于自动求导。

框架应包括：版本化数据层、动力学层、受约束参数层、训练器、科学验证层、证据记录。评估神经活动、行为、稳定性和原始结构偏离；保留多个候选模型，避免把一组拟合参数当作唯一生理真值。

必要对照：随机/重排结构、冻结脑、只训练控制器、不使用脑、同等预算普通 RNN/SNN。测试留出刺激与神经干预，不只看训练任务分数。

## 资源与风险

- 原始论文报告：单线程模拟 1 秒糖刺激约 5 分钟；本机耗时必须实测。
- 原代码默认 30 次重复、使用所有核心。本阶段先采用单进程、低重复数，避免内存挤占。
- 不下载电镜原始体数据，只使用处理后的连接表。
- 稀疏连接、神经状态、时间展开和全脑监视器均有成本；可仿真不等于可低成本端到端训练。
- v630/v783/MaleCNS 的神经元编号和数据格式不可混用。
- 稳定性、参数退化和外部控制器代偿是训练阶段的重要风险。

## 本地项目约定

- `upstream/`：原作者仓库，固定提交，只读使用，不改源码。
- `.venv/`：项目独立 Python 环境。
- `scripts/`：实验入口和结果校验。
- `results/`：可审计的运行配置、数据及结果（不覆盖已有实验）。
- `docs/`：复现记录和结论。
- 临时下载/构建产物放临时目录，不混入交付。

工作区指南已读。其指定的 `../workspace-agent-guide/sync.py` 与 README 在当前挂载工作区不存在，因此暂不能执行指南同步；不手工伪造同步副本，不影响独立复现实验。

## 主要资料

- 原始全脑论文：https://www.nature.com/articles/s41586-024-07763-9
- 原始代码：https://github.com/philshiu/Drosophila_brain_model
- FlyWire 图谱：https://www.nature.com/articles/s41586-024-07558-y
- Flyvis 论文：https://www.nature.com/articles/s41586-024-07939-3
- Flyvis：https://turagalab.github.io/flyvis/
- Brian2：https://brian2.readthedocs.io/en/stable/
- Brian2CUDA：https://brian2cuda.readthedocs.io/
- Brian2modelfitting：https://brian2modelfitting.readthedocs.io/
- snnTorch：https://snntorch.readthedocs.io/
- BrainPy：https://github.com/brainpy/BrainPy
- BrainTrace：https://braintrace.readthedocs.io/

## 执行状态

- [x] 资料调研和计划落盘。
- [x] 固定源码/数据与独立环境（核心数值版本与原完整环境一致；PyArrow 11→17 修复退出崩溃）。
- [x] 全脑短时 smoke test（100 ms，单次）。
- [x] 无输入、糖、糖＋苦味对照实验（各 3 次、每次 1 秒，单进程）。
- [x] 校验结果、记录偏差和下一步；固定种子独立进程复跑的全部脉冲及文件哈希一致。

执行结果见 [docs/REPRODUCTION.md](docs/REPRODUCTION.md)，使用说明见 [README.md](README.md)。MN9 平均放电率分别为 0、67.67、6.67 Hz；上游自带糖刺激 30 次参考均值为 67.03 Hz。当前阶段完成的是小样本定性基线，不是整篇论文的全部复现。

后续尚未执行：原三条件的 30 次重复与刺激频率扫描、梳理行为验证、全脑模型参数敏感性/拟合、路线 B、身体闭环。

## 新研究分支：habituation（2026-09-16）

用户追加授权：建立 `habituation/` 科研目录，通过文献、建模和实际实验形成结论。本轮不自动提交或推送 GitHub。

- [x] 整理[文献与范式选择](habituation/LITERATURE.md)和[预先规定的研究方案](habituation/RESEARCH_PLAN.md)。
- [x] 不局限于糖，选择已有正常传递证据的 JO-CE 机械感觉回路，以 aBN1 为主要神经读出。
- [x] 构建连续状态全脑入口，比较原始固定连接与局部 STD；通过零 U 等价性和状态恢复重放检查。
- [x] 完成 2 模型 × 5 输入实现 × 2 间隔的 20 条序列，每条含 12 次训练与两个独立恢复探测。
- [x] 回读 60 个原始事件文件、重算 280 个响应窗口，验证实际感觉脉冲逐事件一致；10 项单元测试通过。
- [x] 写入[条件性结论](habituation/RESULTS.md)：固定连接模型无递减；局部 STD 在短间隔产生约 90% 递减与休息恢复，长间隔效应较弱。

这不是完整动物习惯化验证，也未证明真实连接组优于简单模型。下一阶段优先比较抑制可塑性、恢复与干预，而不是直接开展全脑大规模参数训练。保留中断与接续记录，不把部分失败片段当作完整实验。

## Habituation阶段二：闭环研究（2026-09-19）

用户追加授权持续执行假设—设计—实验—分析—修正的研究循环，直至阶段性报告。已按本地冻结方案完成三轮，未自动发布或提交。

- [x] 5个输入实现的模型内状态干预，90个窗口：资源恢复救援响应，快状态清除无救援；双重恢复与初次事件完全一致，旁路能力保留。
- [x] 对两个低维代理各拟合两个读出系数，仅使用旧短间隔60个窗口；80个其他旧窗口标为回顾性检查。
- [x] 冻结参数及102个新窗口的逐点预测，再按模型分歧从18个候选选两个新协议，另加新种子对照及9个M0初态窗口。
- [x] 新数据检验：资源代理在3/3协议达到MAE≤1.5，积累代理仅1/3；未利用新输出重新拟合。
- [x] 独立回读本阶段126个事件文件、201个正式窗口，记录工程失败和事后探索性反弹诊断。
- [x] 形成[阶段报告](habituation/stage2/REPORT.md)和[LaTeX PDF](habituation/stage2/report.pdf)，第一版报告保留不变。

科学边界：代理比较不是真实果蝇机制鉴别；全脑生理参数未拟合；仍未证明完整行为习惯化或真实连接结构不可替代。新H4针对局部反弹的时序、空间结构及重置非线性，下一轮实验尚未执行。

## 经典hallmarks补测（2026-09-19）

用户追加要求测试有条件验证的特征。已在独立的 [hallmarks/](habituation/hallmarks/README.md) 中完成，未改变M1参数或上游。

- [x] 新输入实现3701–3703，连续三轮训练、五点独立恢复、150/200Hz强度比较、12→24次平台后训练。
- [x] 两个真实上游候选感觉群体B，配备等时休息、未训练B→A和B→空白控制；未使用人工资源重置替代去习惯化。
- [x] 原10秒H3较严格筛查未获支持；文献驱动、提前冻结的2秒部分恢复补充为3/3，明确保留残余状态解释。
- [x] 原H4(b)比较资格不足，保留结果并冻结一次T=0.75秒、24次补测。补测3/3具资格，F50未分辨快慢；首次80%仅1/3高频更早，不宣称稳健成立。
- [x] H5方向3/3一致、2/3达到预设效应量；H6恢复计数完全相同；两种B均未产生H8增益；H7缺少可比较基线，H9前提不足，H10未测。
- [x] 独立回读1026个原始事件文件、525个观测窗口（含6个空白检查窗），519次实际感觉事件序列全部一致；总成功计算56.58分钟，完成后停止扩大。
- [x] 形成[文字结果](habituation/hallmarks/REPORT.md)及[6页PDF](habituation/hallmarks/report.pdf)，旧报告和原始结果保留。

经典H4与阶段二“局部反弹来源”的假设H4不是同一编号体系，后者仍未执行。新补测支持部分神经习惯化样特征，不等于完整动物行为或真实机制确证；没有提交或推送新研究。
